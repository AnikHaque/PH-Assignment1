<!-- Simple README Style Explanation for keyof -->

<h1>🔑 What is <code>keyof</code> in TypeScript?</h1>

<p>
  <code>keyof</code> হলো TypeScript-এর একটি ছোট কিন্তু দরকারী কীওয়ার্ড। 
  এটা কোনো অবজেক্ট টাইপের সবগুলো প্রপার্টির নাম (keys) বের করে 
  সেগুলোকে একটি ইউনিয়ন টাইপ হিসেবে রিটার্ন করে। 
  এতে কোড আরও সেফ থাকে এবং ভুল key ব্যবহার করার সুযোগ কমে যায়।
</p>

<hr />

<h2>📌 Basic Example</h2>

<pre>
<code>
type User = {
  name: string;
  age: number;
  email: string;
};

type UserKeys = keyof User;
// Result: "name" | "age" | "email"
</code>
</pre>

<p>
  উপরের উদাহরণে <code>keyof User</code> লেখার মানে হলো User টাইপের সব প্রপার্টির নামকে 
  একসাথে টাইপ হিসেবে পাওয়া—যা হলো <code>"name" | "age" | "email"</code>.
</p>

<hr />

<h2>🎯 Why is <code>keyof</code> Useful?</h2>

<ul>
  <li>Object-এর ভুল key ব্যবহার আটকায়</li>
  <li>টাইপ সেফটি বাড়ে</li>
  <li>Generic function তৈরি করতে খুব কাজে লাগে</li>
</ul>

<hr />

<h2>🔍 Real Usage Example</h2>

<pre>
<code>
function getValue&lt;T, K extends keyof T&gt;(obj: T, key: K) {
  return obj[key];
}

const user = { name: "Afsana", age: 22 };

const result = getValue(user, "name"); // ✅ valid
getValue(user, "email"); // ❌ Error: Property 'email' does not exist
</code>
</pre>

<hr />

<h2>✔️ Summary</h2>

<p>
  <code>keyof</code> ব্যবহার করে তুমি কোনো টাইপের সব key-কে একসাথে একটি টাইপ হিসেবে পেতে পারবে।  
  এটা কোডকে নিরাপদ করে এবং জেনেরিক ফাংশন, ইউটিলিটি টাইপ ইত্যাদিতে গুরুত্বপূর্ণ ভূমিকা রাখে।
</p>
